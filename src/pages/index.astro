---
import BaseLayout from '../layouts/BaseLayout.astro';
import content from '../content_html/index.en.html?raw';
import css from '../styles/pages/index.css?raw';
import refs from '../content_html/index.refs.json';
---
<BaseLayout title={'Rosetta Quantum — We measure whether quantum wins, and publish the answer'} description={'The public archive of sealed measurements, including the ones we lose.'} lang="en" altUrl="/es/" pageCss={css} scripts={refs}>
  <Fragment set:html={content} />
</BaseLayout>
