# Archivero — congelado

Este archivo estaba vacío desde el primer commit (`699c0d8`, 21-ago). No es que se haya
perdido contenido: nunca se escribió. El estado real de esta pieza vive en
[`CONGELADO.md`](./CONGELADO.md), en esta misma carpeta.

**Las «cuatro trampas»** que menciona el mensaje de aquel commit no están perdidas tampoco:
viven en [`../README.md`](../README.md) (el índice de las tres piezas), no acá. La de
[`instrumento/README.md`](../instrumento/README.md) documenta tres porque se escribió antes
de encontrar la cuarta (conversión sRGB→lineal de `THREE.Color`); la de arriba las tiene
las cuatro.
